# Examples

The files in this directory contain synthetic or de-identified protocol samples. They contain no
device addresses or captures.

Decode all sample frames:

```bash
python examples/decode_samples.py
```

For live read-only access, use `tools/ss5_ble.py`. The repository intentionally does not provide a
generic arbitrary-write example.

