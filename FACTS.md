# SoundSticks 5 逆向事实清单

更新：2026-09-03

本文是仓库的最高优先级事实基线。旧笔记、脚本注释或实验摘要与本文冲突时，以本文的
保守表述为准。当前证据主要来自一台普通版（非 Wi-Fi 版）SoundSticks 5；其他地区、SKU
和固件需要重新验证。

## 分级定义

- **CONFIRMED**：有直接抓包，并通过主动操作、ACK、状态回推或重复对照验证。
- **OBSERVED**：在当前测试设备或环境中直接观察到，但样本或对照不足以推广。
- **INFERRED**：与现象一致的解释，但没有直接证据证明内部实现或完整语义。
- **UNKNOWN**：材料不足，不能得出结论。
- **SUPERSEDED**：旧结论已被后续证据推翻，或被证明表述过强。

# CONFIRMED

## 蓝牙架构

- 私有设备控制协议运行在 BLE GATT 上；命令、查询、ACK 和状态通知均通过
  characteristics 传输。
- 音频使用经典蓝牙 A2DP，元数据和绝对音量来自 AVRCP。部分音频状态通过 BLE 控制
  通知透传。因此设备不是 BLE-only。
- BLE 控制连接不要求系统级 BLE bond。恢复出厂后，不执行 App 产品匹配流程，测试客户
  端仍曾建立 GATT 并完成控制。
- App 产品匹配不是 BLE SMP 配对的同义词；现有对应抓包中没有观察到 SMP 配对交换。
- BLE 使用会轮换的随机私有地址（RPA）。旧地址可能创建出系统对象但无法枚举服务，客户
  端不得硬编码或长期缓存地址。

## GATT 控制通道

- 主控制 Service UUID：`65786365-6c70-6f69-6e74-2e636f6d0000`。
- 命令 characteristic：`65786365-6c70-6f69-6e74-2e636f6d0002`，观察到
  write/write-without-response。
- 通知 characteristic：`65786365-6c70-6f69-6e74-2e636f6d0001`，观察到 read/notify。
- 某次抓包中的命令、通知、CCCD handle 分别为 `0x03EA`、`0x03EC`、`0x03ED`。handle
  不是稳定接口，跨固件或会话必须使用 UUID。
- 向 CCCD 写 `01 00` 可启用通知。

## 帧和 TLV

- 普通应用帧格式是 `AA CMD LEN DATA`，`LEN` 是 `DATA` 的字节数。
- 查询帧可使用零长度 DATA，例如 `aa 31 00`、`aa 41 00`、`aa e1 00`。
- 多类请求/响应的 DATA 以 `00` 开始，其后为 `TAG LEN VALUE` TLV 链。
- 成功 ACK 反复观察为 `aa 00 02 <acknowledged-cmd> 00`。
- `0xad` 类型的内部日志响应可以包含嵌套 AA 帧，不能无条件按单一 TLV 链解析。

## 灯光

- `aa 31 00` 查询灯光状态；`0x32` 返回或推送灯光状态。
- 设置使用 `cmd 0x33`：
  - `tag 0x99`：灯开关，`00=关`、`01=开`；
  - `tag 0x45`：亮度，实测参数范围 `0..100`；
  - `tag 0x4d`：速度，`01=低`、`02=中`、`03=高`；
  - `tag 0x4f`：当前主题 ID；
  - `tag 0x4c`：`<主题 ID><颜色值>`，颜色值实测为 `0..100`。
- 已验证主题 ID：`0x10` ocean、`0x11` aurora、`0x12` blossom、`0x13` sunrise、
  `0x14` fireplace、`0x15` static。
- 设置成功可收到 ACK 和/或 `0x32` 状态回推。

## 均衡器

- `aa e1 00` 查询当前 EQ，响应命令为 `0xe2`。
- `cmd 0xe3` 设置 7 段 EQ；抓包中的增益使用 little-endian float32。
- `0xe2/0xe3` 在 `LEN` 后有一个不计入长度的 `00` 分隔字节。实测 `e3` 为
  `aa e3 61 00 <97-byte body>`，`e2` 为 `aa e2 62 00 <98-byte body>`。
- `e3` 的 97 字节主体头部是 `c2 07 80 bb 00 00`：7 段、48000 Hz；其后七段均为
  13 字节 `filter type + gain/frequency/Q float32 LE`。filter type 依次为
  `0,1,1,1,1,1,2`，与 App 内置的 low-shelf、五个 peaking EQ、high-shelf 配置一致。
- `e2` 的 DATA 比 `e3` 主体多一个 `c2` 响应前缀，之后结构相同。
- 已对齐的中心频率为 125、250、500、1000、2000、4000、8000 Hz。
- 当前测试设备恢复默认后七段增益均为 `0.0 dB`。

## 综合状态

- `aa 41 00` 查询综合状态；`0x42` 返回或主动推送综合状态。
- `0x42/tag 0x41`：播放状态；当前实测 `01=暂停/空闲`、`02=播放中`。连接初始化
  窗口可能短暂出现变化，单帧不应脱离上下文解释。
- `0x42/tag 0x42`：正常播放场景下与 AVRCP Absolute Volume 同步。
- 在 A2DP 已连接但无 sink input、未播放音频时，两个间隔执行的 AVRCP `VolumeDown`
  分别让该值从 `0x12 → 0x0f → 0x0c`，每步减少 3；继续间隔降低后读到 `0x00`。
  在 `0x00` 再执行一次降低仍读回 `0x00`。
- 在蓝牙 sink 强制静音、软件音量 0%、无 sink input 的条件下，从 0 单次执行 AVRCP
  `VolumeUp` 后该值为 3；逐档升高后最大读回 100，在 100 再升仍为 100。随后已降回 0。
  因此私有 BLE 字段是可直接用于第三方音量条的 `0..100` 百分比，当前设备相对按键步长
  为 3。AVRCP 传输层标准的 `0..127` 标尺已由设备/协议桥接换算，不应再次套用于该字段。
- HK One 在自身没有连接 A2DP、另一台已静音设备保持 A2DP/AVRCP 连接时，通过私有 BLE
  写入 `aa 43 04 00 42 01 VV` 可直接设置硬件音量百分比。实抓 `VV=03` 与 `VV=00` 均收到
  `aa 00 02 43 00` 成功 ACK 和 `aa 42 04 00 42 01 VV` 状态推送；停止 App 后由另一 BLE
  central 独立查询也分别读回 3 和最终的 0。
- `0x42/tag 0x44`：曲名，UTF-8。
- `0x42/tag 0x45`：艺术家，UTF-8。相同 tag 在 `0x32` 灯光上下文中表示亮度。
- `tag 0x36` 已观察到 `0x01`、`0x02`、`0x09`。它不是可靠的 `matched` 布尔值，也
  不能单独作为实时 A2DP 链路的权威布尔值。

## 待机与唤醒

- 物理灯熄灭、App 停止时，Linux/BlueZ 客户端仍曾通过匿名 RPA 建立 GATT、枚举控制
  服务、订阅并查询。BLE 控制面可以在这种待机表现下保持在线。
- 待机时灯光配置可能仍返回“开”，灯光写命令也可 ACK，但物理灯不亮。因此灯光配置
  状态不等于整机/音频电源状态，灯光命令不能视为独立唤醒命令。
- 有效 AUX 音频能够在不启动 App、不按按钮时唤醒并播放；只插入 AUX 线而没有音频不
  唤醒。
- 已配对 A2DP 设备只建立音频连接、不播放媒体，也能立即唤醒。该结果在 AUX 插入和
  AUX 物理拔除两种条件下重复确认。
- 已观察到更深休眠状态下，BLE/App 本身不足以可靠唤醒；需要 AUX 有效输入、重新开关机，
  或由此前已配对的经典蓝牙音频设备重新建立 A2DP/AVRCP 连接。灯灭这一外观不足以区分
  “BLE 仍可控的待机”和这种深休眠。
- 自动关闭配置为 600 秒且剩余时间已经读为 0 时，音箱仍曾以无名称 RPA 广播 Fast Pair
  service data，并允许仅 GATT 的服务枚举、通知订阅和查询。因此“深休眠必然完全停止
  BLE 广播/GATT”是过强表述；按设备名扫描还会错误漏报。
- 在上述剩余时间为 0 的状态下，依次发送 play、next、previous 均收到 `0x43` 成功 ACK，
  但综合状态持续为暂停/空闲，自动关闭剩余时间仍为 0，经典蓝牙音频连接始终为断开。
  这些媒体动作不能作为可靠的退出休眠/唤醒命令。

## 默认值和持久化

- 当前测试设备恢复默认后观察到：灯开、亮度 100、速度中、主题 ocean、EQ 全 0 dB。
- 六主题默认颜色观察为：`0x10:54`、`0x11:50`、`0x12:75`、`0x13:60`、
  `0x14:72`、`0x15:0`。
- 灯光和 EQ 设置在控制客户端断开后仍可由后续连接读取到，不只保存在客户端进程中。

## App 属性对照

- App 中六个灯效资源/名称与 `0x10..0x15` 已通过单变量操作和 GATT 写入对齐；选择主题
  会同时下发该主题 ID 与默认颜色，但不下发亮度或速度。
- App 的亮度和颜色滑块与协议 `0..100` 参数呈线性对应；UI 交互本身可能无法拖到亮度 0，
  但协议层曾观察到值 0。
- 受控拖动中亮度目标 15/35/55/75/95% 得到 15/35/54/75/95，颜色目标
  25/50/75% 得到 25/50/75。54 和最左端亮度 1 属于 App 触摸坐标量化，不应改变第三方
  UI 直接以整数百分比映射 `0..100` 的实现。
- App 的速度控件是低/中/高三档，与协议值 `1/2/3` 对应。
- App 首页音量条以私有 BLE `0x43/tag 0x42` 写入直接的 `0..100` 整数百分比；不要求
  App 所在设备同时作为 A2DP 音频源。
- HK One 所在的 Android 设备未建立经典蓝牙音频连接时，App 仍可通过 BLE GATT 发现
  已唤醒音箱、读取主控制页并进入产品设置。另一台已配对设备建立 A2DP 连接可以负责
  唤醒；唤醒设备与 BLE 控制设备不必是同一台。
- App EQ 页面包含七个频段：125、250、500、1000、2000、4000、8000 Hz；改变一个
  频段时，`0xe3` 全量快照中对应 little-endian float32 增益改变。
- HK One 2.5.4 前端的 EQ 滑条为 `-12..+12` 共 25 个整数位置，通常每格对应 `0.5 dB`。
  第一段 125 Hz 在负半轴另有 `1.5x` 补偿；该算法已由 App 代码确认，负端点仍待真实
  设备抓包复核。
- App 中某些“重置”操作只先改变页面值，必须再确认才写入设备；因此“点击重置后没有
  GATT 设置帧”不能单独证明重置功能无效。
- App 的 EQ 重置先把七段编辑值归零，点击 `✓` 后发送全零 `0xe3` 完整快照。
- 灯效颜色重置使用 `cmd 0x33/tag 0x50`，格式为 `aa 33 04 00 50 01 <theme>`。六个主题
  均已逐个通过 App 重置按钮、ACK 和完整默认色表状态回推验证；这是单主题颜色重置，
  不是整机恢复出厂。
- App 恢复出厂后读回的灯光状态包含亮度 100、速度 2、主题 `0x10`、灯开，以及六个
  主题默认颜色；这组值已独立写入机器可读参考表。

## 播放与产品设置

- App 播放和暂停分别写入 `aa 43 04 00 41 01 02` 与 `aa 43 04 00 41 01 01`，均收到
  `aa 00 02 43 00` ACK。`0x42/tag 0x41` 的 `02`/`01` 分别表示播放/暂停；当音源端没有
  实际媒体流时，播放命令 ACK 后设备可以很快主动推送 `01` 回到暂停。
- App 上一曲和下一曲分别写入 `aa 43 04 00 41 01 03` 与 `aa 43 04 00 41 01 04`，均收到
  `aa 00 02 43 00` ACK。它们是动作值，不是已确认的持久播放状态值。
- App 重命名提交为 `aa 13 LL 00 c1 NN <UTF-8 name>`，其中 `NN` 是名称字节数、
  `LL=NN+3`；临时改为 `SS5_TEST` 后收到成功 ACK，随后已用同一命令恢复原名。
- 反馈音查询为 `aa f1 00`，状态响应 `aa f2 01 VV`；`VV=00` 为关、`01` 为开。设置为
  `aa f3 01 VV`，设备设置后直接以 `f2` 回推当前状态。开→关→开已做单变量闭环并恢复。
- 自动关闭查询为 `aa b8 00`；响应 `aa b9 04 <configured uint16 LE> <remaining uint16 LE>`，
  两个字段单位均为秒。设置命令为 `aa ba 02 <configured uint16 LE>`，成功 ACK 为
  `aa 00 02 ba 00`。
- 自动关闭五个 App 选项已逐项设置并读回：从不=0、10 分钟=600、1 小时=3600、
  2 小时=7200、4 小时=14400。`从不`读回 `(0,0)`；计时选项读回配置秒数及递减的剩余
  秒数。测试结束已恢复为 10 分钟。
- 一次受控观察中，灯效设置后 10 分钟倒计时读回 573 秒；只读查询后再次读取为 506 秒，
  未回到 600。由此确认普通灯效写操作会重置该次不活动计时，而 `b8` 查询本身不会。

# OBSERVED

- 本轮产品信息页将当前测试设备的软件版本显示为 `26.21.10`；设备序列号不保存在仓库中。
- 枚举中出现 `0xFE2C` 及 `FE2C123x...` characteristics。该 UUID 对应 Google Fast
  Pair，不能据此判断芯片厂商。
- 还观察到 `0x184F`、`e49a1800-f69a-11e8-8eb2-f2801f1b9fd1`、`0xFDDF` 等服务。
  标准 UUID `0x184F` 的名称是 Broadcast Audio Scan Service，但服务存在不证明设备
  完整实现了相关功能。
- 广告不保证携带产品名。至少一次可连接阶段只观察到 Fast Pair `0xFE2C` service data；
  仅按 `SoundSticks` 名称过滤会产生假阴性。
- 一组实验中，App 持有 GATT 时第二客户端仍可同时连接并查询；另一组实验中，App 活跃
  时第二客户端订阅和写入失败。多连接行为可能依赖主机栈、会话阶段或设备状态。
- 跨主机对照中，Linux/BlueZ 和一套 Windows/WinRT 环境成功；两套 Intel/WinRT 环境
  在不同阶段失败。样本不足以宣称所有 Intel 控制器不兼容，也不能只归因于虚拟机 USB
  直通。
- A2DP 播放通常比 AUX 具有更高音源优先级；流停止后可回退到 AUX。该行为尚未由一个
  独立协议字段完整解释。
- 手机保持蓝牙开启并连接其他外设时，SoundSticks 5 的 A2DP 链路在长时间无媒体活动后
  消失，音箱回到灯灭待机。精确超时及断开发起方未记录。
- 当前普通版产品设置页直接观察到“自动关闭、反馈音、重命名、恢复出厂设置”。
- RPA 在 App 断开、进程结束或恢复出厂附近多次变化，但唯一触发条件和轮换周期未知。

# INFERRED

- `0x36` 更像上下文相关的连接/产品状态枚举；完整状态机未知。
- 相反的多连接结果更像主机蓝牙栈、会话所有权和设备状态共同作用，而不是固定的“只能
  一个 BLE central”。
- 输入源选择可能主要由 A2DP/AUX 活性自动决定；尚未发现可靠的 App 主动切源写命令。
- 灯光和 EQ 配置可能保存在设备持久存储中；目前只确认跨客户端断开保持，未系统验证
  断电保持周期。
- `aa f5 02 01 1e` 可能开启约 30 秒的 App 产品匹配窗口；参数语义仍未确认。

# UNKNOWN

- 内部 SoC 厂商、是否使用 ESP32、具体芯片型号和各芯片职责。
- 普通版是否具有可用 Wi-Fi 硬件/固件，以及与 Wi-Fi 版的硬件关系。
- `e49a1800...`、`0xFDDF` 和 `0x184F` 在本设备上的具体用途。
- `0x36` 的完整枚举与 `0x09` 的精确定义。
- App/第二客户端冲突发生在设备固件、主机 GATT 栈、连接参数还是业务会话仲裁层。
- 最大并发 GATT link 数量，以及两个非官方客户端能否同时完整控制。
- Android、Windows、BlueZ 的 AVRCP 绝对音量请求在非整 3 档位时分别如何取整，以及
  私有 BLE 任意整数与各平台音量 UI 的双向同步细节。
- 精确休眠超时、BLE 常连接是否阻止休眠、未 bond 客户端是否存在独立唤醒能力。
- `0x43` 除已确认 `tag 0x41` 四种媒体动作和 `tag 0x42` 音量写之外的字段、设备信息字段、`0xa2`
  等未解字段的完整语义。
- 重命名对经典蓝牙广播名、各平台名称缓存及断电持久化的完整影响。
- 恢复出厂和解绑等高风险操作的协议和安全边界。OTA/固件传输明确不在本仓库范围。
- 其他地区、SKU、Wi-Fi 版和未来固件的兼容性。

# SUPERSEDED

- **“进入深度休眠后必然不再广播 BLE，也无法建立 GATT。”** 表述过强；自动关闭剩余
  时间为 0 时仍观察到匿名 Fast Pair 广播和可用 GATT，但媒体命令不能据此唤醒音频部分。
- **“SoundSticks 5 是 BLE-only，没有经典蓝牙。”** 已被 A2DP 播放、AVRCP 音量和
  元数据实测推翻。
- **“经典 inquiry 扫不到就证明没有 BR/EDR。”** 扫描状态和可发现模式会影响结果；
  实际 A2DP 连接构成反证。
- **“严格只允许一个 BLE central。”** 曾观察到两个 GATT link 共存。
- **“支持两个 link，所以 App 和任意客户端始终可以同时完整控制。”** 表述过强；另有
  订阅和写入失败的受控观察。
- **“`0x36=0x02` 表示 matched，可解析为布尔值。”** 已被 A2DP 与待机对照推翻。
- **“`0x36` 是 BLE 控制连接状态。”** 已被纯 A2DP 事件触发的状态变化推翻。
- **“`0xFE2C` 是 ESP-BLE-MESH。”** 错误；它是 Google Fast Pair Service UUID。
- **“`0x184F` 是 ESP OTA。”** 错误；Bluetooth SIG 将其分配为 Broadcast Audio Scan
  Service。
- **“出现 `0xFE2C`、`0x184F` 或厂商 UUID 就证明主控是 ESP32。”** 证据不足。
- **“Windows 连接一次就会锁死音箱。”** 表述过强；RPA 轮换、信号、对象生命周期和
  主机栈均可造成看似相同的失败。
- **“App 占用时只有通知失败，写命令始终可用。”** 后续矩阵观察到两者均可失败。

## 外部编号参考

- Google Fast Pair `0xFE2C`：
  <https://developers.google.com/nearby/fast-pair/specifications/characteristics>
- Bluetooth SIG Assigned Numbers：
  <https://www.bluetooth.com/specifications/assigned-numbers/>
